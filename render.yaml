services:
  - type: web
    name: ai-automations
    runtime: node
    rootDir: sales-followup
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
