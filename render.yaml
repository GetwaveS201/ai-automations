services:
  - type: web
    name: ai-automations
    runtime: node
    rootDir: sales-followup
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
